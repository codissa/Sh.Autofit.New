<UserControl x:Class="Sh.Autofit.New.PartsMappingUI.Views.VehicleDataSyncView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200"
             FlowDirection="RightToLeft">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Loading Overlay -->
        <Border Grid.Row="0" Grid.RowSpan="5"
               Background="#80000000"
               Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"
               Panel.ZIndex="1000">
            <Border Background="White"
                   CornerRadius="8"
                   Padding="30"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   MinWidth="400">
                <StackPanel>
                    <TextBlock Text="{Binding StatusMessage}"
                              FontSize="14"
                              FontWeight="SemiBold"
                              HorizontalAlignment="Center"
                              TextWrapping="Wrap"
                              TextAlignment="Center"/>

                    <ProgressBar Height="20"
                                Margin="0,15,0,10"
                                Minimum="0"
                                Maximum="{Binding TotalRecords}"
                                Value="{Binding Progress}"/>

                    <TextBlock HorizontalAlignment="Center"
                              FontSize="12"
                              Foreground="Gray">
                        <Run Text="{Binding Progress, Mode=OneWay, StringFormat=N0}"/>
                        <Run Text="/"/>
                        <Run Text="{Binding TotalRecords, Mode=OneWay, StringFormat=N0}"/>
                    </TextBlock>

                    <Button Content="ביטול"
                           Command="{Binding CancelSyncCommand}"
                           Margin="0,15,0,0"
                           Padding="20,8"
                           Background="#F44336"
                           Foreground="White"
                           BorderThickness="0"
                           Cursor="Hand"
                           Visibility="{Binding CanCancel, Converter={StaticResource BoolToVisibilityConverter}}"/>
                </StackPanel>
            </Border>
        </Border>

        <!-- Title -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="סנכרון נתוני רכבים ממשלתיים"
                      FontSize="28"
                      FontWeight="Bold"/>
            <TextBlock Text="הורדת כל מאגרי הרכבים הממשלתיים למסד נתונים מקומי לחיפוש מהיר"
                      FontSize="14"
                      Foreground="Gray"
                      Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Stats Panel -->
        <Border Grid.Row="1"
               Background="#E3F2FD"
               CornerRadius="8"
               Padding="15"
               Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Vehicle Types -->
                <StackPanel Grid.Column="0" Margin="5">
                    <TextBlock Text="סוגי רכב" FontSize="11" Foreground="#666"/>
                    <TextBlock Text="{Binding VehicleTypeCount}" FontSize="20" FontWeight="Bold" Foreground="#1565C0"/>
                </StackPanel>

                <!-- Consolidated Models -->
                <StackPanel Grid.Column="1" Margin="5">
                    <TextBlock Text="דגמים מאוחדים" FontSize="11" Foreground="#666"/>
                    <TextBlock Text="{Binding ConsolidatedModelCount}" FontSize="20" FontWeight="Bold" Foreground="#1565C0"/>
                </StackPanel>

                <!-- WLTP -->
                <StackPanel Grid.Column="2" Margin="5">
                    <TextBlock Text="מפרטי WLTP" FontSize="11" Foreground="#666"/>
                    <TextBlock Text="{Binding WltpStats}" FontSize="12" Foreground="#333" TextWrapping="Wrap"/>
                </StackPanel>

                <!-- Quantities -->
                <StackPanel Grid.Column="3" Margin="5">
                    <TextBlock Text="כמויות רכבים" FontSize="11" Foreground="#666"/>
                    <TextBlock Text="{Binding QuantityStats}" FontSize="12" Foreground="#333" TextWrapping="Wrap"/>
                </StackPanel>

                <!-- Registrations -->
                <StackPanel Grid.Column="4" Margin="5">
                    <TextBlock Text="רישומי רכבים" FontSize="11" Foreground="#666"/>
                    <TextBlock Text="{Binding RegistrationStats}" FontSize="12" Foreground="#333" TextWrapping="Wrap"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Action Buttons -->
        <Border Grid.Row="2"
               Background="#F5F5F5"
               CornerRadius="8"
               Padding="15"
               Margin="0,0,0,15">
            <WrapPanel Orientation="Horizontal">
                <!-- Sync All (incremental) -->
                <Button Command="{Binding SyncAllDataCommand}"
                       Padding="15,10"
                       FontSize="14"
                       FontWeight="SemiBold"
                       Background="#4CAF50"
                       Foreground="White"
                       BorderThickness="0"
                       Cursor="Hand"
                       Margin="0,0,10,5">
                    <TextBlock Text="סנכרון כל המאגרים"/>
                    <Button.ToolTip>
                        <TextBlock Text="סנכרון אינקרמנטלי — מוריד רק רשומות חדשות" TextWrapping="Wrap" MaxWidth="300"/>
                    </Button.ToolTip>
                </Button>

                <!-- Full Refresh -->
                <Button Command="{Binding FullRefreshCommand}"
                       Padding="15,10"
                       FontSize="14"
                       Background="#FF9800"
                       Foreground="White"
                       BorderThickness="0"
                       Cursor="Hand"
                       Margin="0,0,10,5">
                    <TextBlock Text="רענון מלא"/>
                    <Button.ToolTip>
                        <TextBlock Text="מוחק את כל הנתונים המקומיים ומוריד הכל מחדש" TextWrapping="Wrap" MaxWidth="300"/>
                    </Button.ToolTip>
                </Button>

                <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" Margin="5,0"/>

                <!-- Individual sync buttons -->
                <Button Command="{Binding SyncWltpOnlyCommand}"
                       Content="מפרטי WLTP"
                       Padding="12,8"
                       FontSize="12"
                       Background="#2196F3"
                       Foreground="White"
                       BorderThickness="0"
                       Cursor="Hand"
                       Margin="0,0,8,5"/>

                <Button Command="{Binding SyncQuantitiesOnlyCommand}"
                       Content="כמויות"
                       Padding="12,8"
                       FontSize="12"
                       Background="#2196F3"
                       Foreground="White"
                       BorderThickness="0"
                       Cursor="Hand"
                       Margin="0,0,8,5"/>

                <Button Command="{Binding SyncRegistrationsOnlyCommand}"
                       Content="רישומי רכבים"
                       Padding="12,8"
                       FontSize="12"
                       Background="#2196F3"
                       Foreground="White"
                       BorderThickness="0"
                       Cursor="Hand"
                       Margin="0,0,8,5"/>
            </WrapPanel>
        </Border>

        <!-- Status and Results -->
        <Grid Grid.Row="3" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                   Background="White"
                   BorderBrush="#E0E0E0"
                   BorderThickness="1"
                   CornerRadius="8"
                   Padding="15">
                <StackPanel>
                    <TextBlock Text="סטטוס נוכחי"
                              FontSize="14"
                              FontWeight="SemiBold"
                              Margin="0,0,0,10"/>
                    <TextBlock Text="{Binding StatusMessage}"
                              FontSize="13"
                              TextWrapping="Wrap"
                              Foreground="#333"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2"
                   Background="White"
                   BorderBrush="#E0E0E0"
                   BorderThickness="1"
                   CornerRadius="8"
                   Padding="15">
                <StackPanel>
                    <TextBlock Text="תוצאות סנכרון אחרון"
                              FontSize="14"
                              FontWeight="SemiBold"
                              Margin="0,0,0,10"/>
                    <TextBlock Text="{Binding SyncResultSummary}"
                              FontSize="13"
                              TextWrapping="Wrap"
                              Foreground="#333"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Detailed Results -->
        <Border Grid.Row="4"
               Background="White"
               BorderBrush="#E0E0E0"
               BorderThickness="1"
               CornerRadius="8"
               Padding="15">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="פרטי סנכרון מלאים"
                              FontSize="14"
                              FontWeight="SemiBold"
                              Margin="0,0,0,10"/>
                    <TextBlock Text="{Binding SyncDetails}"
                              FontFamily="Consolas"
                              FontSize="12"
                              TextWrapping="Wrap"
                              Foreground="#333"/>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
