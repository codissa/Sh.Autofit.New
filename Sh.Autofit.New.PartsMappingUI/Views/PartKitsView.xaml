<UserControl x:Class="Sh.Autofit.New.PartsMappingUI.Views.PartKitsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1200"
             FlowDirection="RightToLeft">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="350"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left Panel - Kits List -->
        <Border Grid.Column="0" BorderBrush="#E0E0E0" BorderThickness="0,0,1,0" Padding="0,0,10,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Title -->
                <TextBlock Grid.Row="0"
                          Text="ערכות חלקים"
                          FontSize="20"
                          FontWeight="Bold"
                          Margin="0,0,0,15"/>

                <!-- Action Buttons -->
                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
                    <Button Content="➕ ערכה חדשה"
                            Command="{Binding CreateKitCommand}"
                            Padding="10,6"
                            Margin="0,0,5,0"
                            Background="#4CAF50"
                            Foreground="White"
                            BorderThickness="0"
                            FontSize="12"
                            Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    CornerRadius="4"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#45A049"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <Button Content="✏️ ערוך"
                            Command="{Binding EditKitCommand}"
                            Padding="10,6"
                            Margin="0,0,5,0"
                            Background="#2196F3"
                            Foreground="White"
                            BorderThickness="0"
                            FontSize="12"
                            Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    CornerRadius="4"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#1976D2"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <Button Content="📑 שכפל"
                            Command="{Binding DuplicateKitCommand}"
                            Padding="10,6"
                            Margin="0,0,5,0"
                            Background="#FF9800"
                            Foreground="White"
                            BorderThickness="0"
                            FontSize="12"
                            Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    CornerRadius="4"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#F57C00"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <Button Content="🗑️ מחק"
                            Command="{Binding DeleteKitCommand}"
                            Padding="10,6"
                            Background="#F44336"
                            Foreground="White"
                            BorderThickness="0"
                            FontSize="12"
                            Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    CornerRadius="4"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#D32F2F"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>

                <!-- Search Box -->
                <TextBox Grid.Row="2"
                         Text="{Binding KitSearchText, UpdateSourceTrigger=PropertyChanged}"
                         Padding="8"
                         Margin="0,0,0,10">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Style.Triggers>
                                <Trigger Property="Text" Value="">
                                    <Setter Property="Background">
                                        <Setter.Value>
                                            <VisualBrush Stretch="None" AlignmentX="Right">
                                                <VisualBrush.Visual>
                                                    <TextBlock Text="🔍 חפש ערכות..."
                                                              Foreground="Gray"
                                                              Margin="0,0,5,0"
                                                              FlowDirection="RightToLeft"/>
                                                </VisualBrush.Visual>
                                            </VisualBrush>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>

                <!-- Kits List -->
                <ListBox Grid.Row="3"
                         ItemsSource="{Binding Kits}"
                         SelectedItem="{Binding SelectedKit}"
                         BorderThickness="0">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="10"
                                    Margin="0,2"
                                    Background="#FAFAFA"
                                    CornerRadius="4"
                                    BorderBrush="#E0E0E0"
                                    BorderThickness="1">
                                <StackPanel>
                                    <TextBlock FontWeight="SemiBold" FontSize="13">
                                        <Run Text="{Binding StatusIcon, Mode=OneWay}"/>
                                        <Run Text="{Binding KitName}"/>
                                    </TextBlock>
                                    <TextBlock Text="{Binding Description}"
                                              Foreground="#666"
                                              FontSize="11"
                                              Margin="0,3,0,0"
                                              TextWrapping="Wrap"
                                              MaxHeight="40"/>
                                    <TextBlock Foreground="Gray" FontSize="10" Margin="0,3,0,0">
                                        <Run Text="חלקים: "/>
                                        <Run Text="{Binding PartCount}"/>
                                        <Run Text=" | נוצר: "/>
                                        <Run Text="{Binding CreatedAtDisplay, Mode=OneWay}"/>
                                    </TextBlock>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>

        <!-- Right Panel - Kit Details -->
        <Grid Grid.Column="1" Margin="10,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Kit Details Header -->
            <Border Grid.Row="0"
                    Background="#F5F5F5"
                    Padding="15"
                    Margin="0,0,0,15"
                    CornerRadius="4"
                    Visibility="{Binding SelectedKit, Converter={StaticResource BoolToVisibilityConverter}}">
                <StackPanel>
                    <TextBlock FontSize="18" FontWeight="Bold">
                        <Run Text="{Binding SelectedKit.KitName}"/>
                    </TextBlock>
                    <TextBlock Text="{Binding SelectedKit.Description}"
                              Foreground="#666"
                              FontSize="13"
                              Margin="0,5,0,0"
                              TextWrapping="Wrap"/>
                </StackPanel>
            </Border>

            <!-- Parts Management Buttons -->
            <StackPanel Grid.Row="1"
                       Margin="0,0,0,10"
                       Visibility="{Binding SelectedKit, Converter={StaticResource BoolToVisibilityConverter}}">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                    <Button Content="➕ הוסף חלקים"
                            Command="{Binding AddPartsToKitCommand}"
                            Padding="12,6"
                            Margin="0,0,10,0"
                            Background="#4CAF50"
                            Foreground="White"
                            BorderThickness="0"
                            FontSize="12"
                            Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    CornerRadius="4"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#45A049"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <Button Content="🔄 סנכרן ערכה זו"
                            Command="{Binding SyncKitCommand}"
                            Padding="12,6"
                            Margin="0,0,10,0"
                            Background="#9C27B0"
                            Foreground="White"
                            BorderThickness="0"
                            FontSize="12"
                            Cursor="Hand"
                            ToolTip="מפה את כל החלקים בערכה לכל הרכבים המשויכים לכל אחד מהחלקים">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    CornerRadius="4"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#7B1FA2"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <Button Content="🔄 סנכרן כל הערכות"
                            Command="{Binding SyncAllKitsCommand}"
                            Padding="12,6"
                            Margin="0,0,10,0"
                            Background="#673AB7"
                            Foreground="White"
                            BorderThickness="0"
                            FontSize="12"
                            Cursor="Hand"
                            ToolTip="מסנכרן את כל הערכות במערכת">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    CornerRadius="4"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#512DA8"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <TextBlock Text="{Binding StatusMessage}"
                              VerticalAlignment="Center"
                              Foreground="#666"
                              FontSize="12"
                              Margin="10,0,0,0"/>
                </StackPanel>
            </StackPanel>

            <!-- Parts in Kit -->
            <DataGrid Grid.Row="2"
                     ItemsSource="{Binding KitParts}"
                     AutoGenerateColumns="False"
                     IsReadOnly="True"
                     CanUserAddRows="False"
                     CanUserDeleteRows="False"
                     SelectionMode="Single"
                     GridLinesVisibility="Horizontal"
                     HeadersVisibility="Column"
                     AlternatingRowBackground="#F9F9F9"
                     Visibility="{Binding SelectedKit, Converter={StaticResource BoolToVisibilityConverter}}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="מקט" Binding="{Binding PartItemKey}" Width="120"/>
                    <DataGridTextColumn Header="שם חלק" Binding="{Binding PartName}" Width="*"/>
                    <DataGridTextColumn Header="קטגוריה" Binding="{Binding Category}" Width="150"/>
                    <DataGridTextColumn Header="רכבים" Binding="{Binding VehicleCountDisplay, Mode=OneWay}" Width="100"/>
                    <DataGridTextColumn Header="במלאי" Binding="{Binding StockIcon, Mode=OneWay}" Width="60"/>
                    <DataGridTemplateColumn Header="פעולות" Width="80">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="🗑️"
                                       Command="{Binding DataContext.RemovePartFromKitCommand,
                                                RelativeSource={RelativeSource AncestorType=UserControl}}"
                                       CommandParameter="{Binding}"
                                       Background="#F44336"
                                       Foreground="White"
                                       BorderThickness="0"
                                       Padding="8,4"
                                       Cursor="Hand"
                                       ToolTip="הסר מהערכה">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}"
                                                               CornerRadius="3"
                                                               Padding="{TemplateBinding Padding}">
                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#D32F2F"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>

            <!-- Empty State -->
            <Border Grid.Row="2"
                   Background="White"
                   BorderBrush="#E0E0E0"
                   BorderThickness="2"
                   CornerRadius="8"
                   Padding="40">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectedKit}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="📦" FontSize="72" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                    <TextBlock Text="בחר ערכה מהרשימה לצפייה ועריכה"
                              FontSize="18"
                              FontWeight="Bold"
                              HorizontalAlignment="Center"
                              Foreground="#666"/>
                    <TextBlock Text="או צור ערכה חדשה להתחלה"
                              FontSize="14"
                              HorizontalAlignment="Center"
                              Foreground="#999"
                              Margin="0,10,0,0"/>
                </StackPanel>
            </Border>

            <!-- Loading Indicator -->
            <ProgressBar Grid.Row="3"
                        IsIndeterminate="True"
                        Height="4"
                        Margin="0,10,0,0"
                        Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"/>
        </Grid>
    </Grid>
</UserControl>
