<Window x:Class="Sh.Autofit.New.PartsMappingUI.Views.CreateVirtualPartDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="יצירת חלק וירטואלי" Height="600" Width="600"
        WindowStartupLocation="CenterOwner"
        Background="#ECF0F1">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#3498DB" Padding="20">
            <StackPanel>
                <TextBlock Text="🔶 יצירת חלק וירטואלי"
                          FontSize="20" FontWeight="Bold" Foreground="White"
                          HorizontalAlignment="Right"/>
                <TextBlock Text="צור חלק זמני עד שהחלק האמיתי יגיע למלאי"
                          FontSize="12" Foreground="#ECF0F1" Margin="0,5,0,0"
                          HorizontalAlignment="Right"/>
            </StackPanel>
        </Border>

        <!-- Form -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="30,20">
                <!-- OEM Number 1 (Required) -->
                <TextBlock Text="מספר OEM 1 (חובה - יהפוך למספר חלק) *"
                          FontWeight="Bold" Margin="0,0,0,5"
                          HorizontalAlignment="Right"/>
                <TextBox x:Name="Oem1TextBox"
                        FontSize="14" Padding="8" Margin="0,0,0,15"
                        FlowDirection="LeftToRight"/>

                <!-- OEM Numbers 2-5 (Optional) -->
                <TextBlock Text="מספרי OEM נוספים (אופציונלי)"
                          FontWeight="Bold" Margin="0,0,0,5"
                          HorizontalAlignment="Right"/>

                <TextBox x:Name="Oem2TextBox"
                        FontSize="14" Padding="8" Margin="0,0,0,5"
                        FlowDirection="LeftToRight"/>

                <TextBox x:Name="Oem3TextBox"
                        FontSize="14" Padding="8" Margin="0,0,0,5"
                        FlowDirection="LeftToRight"/>

                <TextBox x:Name="Oem4TextBox"
                        FontSize="14" Padding="8" Margin="0,0,0,5"
                        FlowDirection="LeftToRight"/>

                <TextBox x:Name="Oem5TextBox"
                        FontSize="14" Padding="8" Margin="0,0,0,15"
                        FlowDirection="LeftToRight"
                        ToolTip="אם יש יותר מ-5 מספרי OEM, הפרד אותם ב-/"/>

                <!-- Description (Required) -->
                <TextBlock Text="תיאור (חובה) *"
                          FontWeight="Bold" Margin="0,0,0,5"
                          HorizontalAlignment="Right"/>
                <TextBox x:Name="DescriptionTextBox"
                        FontSize="14" Padding="8" Margin="0,0,0,15"/>

                <!-- Notes (Optional) -->
                <TextBlock Text="הערות (אופציונלי)"
                          FontWeight="Bold" Margin="0,0,0,5"
                          HorizontalAlignment="Right"/>
                <TextBox x:Name="NotesTextBox"
                        FontSize="14" Padding="8" Margin="0,0,0,15"
                        TextWrapping="Wrap" AcceptsReturn="True"
                        Height="100"/>

                <!-- Category (Optional) -->
                <TextBlock Text="קטגוריה (אופציונלי)"
                          FontWeight="Bold" Margin="0,0,0,5"
                          HorizontalAlignment="Right"/>
                <ComboBox x:Name="CategoryComboBox"
                         FontSize="14" Padding="8" Margin="0,0,0,15"
                         IsEditable="True"/>

                <!-- Info Box -->
                <Border Background="#FFF9E6" BorderBrush="#F39C12" BorderThickness="2"
                       Padding="15" Margin="0,10,0,0">
                    <StackPanel>
                        <TextBlock Text="💡 מידע חשוב" FontWeight="Bold" FontSize="14"
                                  HorizontalAlignment="Right" Margin="0,0,0,10"/>
                        <TextBlock TextWrapping="Wrap" HorizontalAlignment="Right">
                            <Run Text="• מספר החלק יהיה המספר OEM 1 שהזנת"/>
                            <LineBreak/>
                            <Run Text="• החלק יופיע בכל חיפושי החלקים עם סמן 🔶"/>
                            <LineBreak/>
                            <Run Text="• כאשר חלק אמיתי עם אותו OEM יגיע, תקבל התראה"/>
                            <LineBreak/>
                            <Run Text="• החלק הוירטואלי יימחק והמיפויים יועברו לחלק האמיתי"/>
                        </TextBlock>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Buttons -->
        <Border Grid.Row="2" Background="White" Padding="20" BorderBrush="#BDC3C7" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <Button Content="✓ צור חלק וירטואלי"
                       Click="CreateButton_Click"
                       Background="#27AE60" Foreground="White"
                       FontSize="14" FontWeight="Bold"
                       Padding="20,10" Margin="0,0,10,0"/>

                <Button Content="✗ ביטול"
                       Click="CancelButton_Click"
                       Background="#95A5A6" Foreground="White"
                       FontSize="14"
                       Padding="20,10"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
