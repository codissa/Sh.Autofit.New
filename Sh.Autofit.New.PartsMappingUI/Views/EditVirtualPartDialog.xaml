<Window x:Class="Sh.Autofit.New.PartsMappingUI.Views.EditVirtualPartDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="עריכת חלק וירטואלי" Height="600" Width="600"
        WindowStartupLocation="CenterOwner"
        Background="#ECF0F1">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#3498DB" Padding="20">
            <StackPanel>
                <TextBlock Text="✏️ עריכת חלק וירטואלי"
                          FontSize="20" FontWeight="Bold" Foreground="White"
                          HorizontalAlignment="Right"/>
                <TextBlock Text="ערוך פרטי החלק הוירטואלי"
                          FontSize="12" Foreground="#ECF0F1" Margin="0,5,0,0"
                          HorizontalAlignment="Right"/>
            </StackPanel>
        </Border>

        <!-- Form -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="30,20">
                <!-- Part Number (Read-Only) -->
                <TextBlock Text="מספר חלק (לא ניתן לשינוי)"
                          FontWeight="Bold" Margin="0,0,0,5"
                          HorizontalAlignment="Right"/>
                <TextBox x:Name="PartNumberTextBox"
                        FontSize="14" Padding="8" Margin="0,0,0,15"
                        IsReadOnly="True"
                        Background="#E8E8E8"
                        FlowDirection="LeftToRight"/>

                <!-- OEM Number 1 (Required) -->
                <TextBlock Text="מספר OEM 1 (חובה) *"
                          FontWeight="Bold" Margin="0,0,0,5"
                          HorizontalAlignment="Right"/>
                <TextBox x:Name="Oem1TextBox"
                        FontSize="14" Padding="8" Margin="0,0,0,15"
                        FlowDirection="LeftToRight"/>

                <!-- OEM Numbers 2-5 (Optional) -->
                <TextBlock Text="מספרי OEM נוספים (אופציונלי)"
                          FontWeight="Bold" Margin="0,0,0,5"
                          HorizontalAlignment="Right"/>

                <TextBox x:Name="Oem2TextBox"
                        FontSize="14" Padding="8" Margin="0,0,0,5"
                        FlowDirection="LeftToRight"/>

                <TextBox x:Name="Oem3TextBox"
                        FontSize="14" Padding="8" Margin="0,0,0,5"
                        FlowDirection="LeftToRight"/>

                <TextBox x:Name="Oem4TextBox"
                        FontSize="14" Padding="8" Margin="0,0,0,5"
                        FlowDirection="LeftToRight"/>

                <TextBox x:Name="Oem5TextBox"
                        FontSize="14" Padding="8" Margin="0,0,0,15"
                        FlowDirection="LeftToRight"
                        ToolTip="אם יש יותר מ-5 מספרי OEM, הפרד אותם ב-/"/>

                <!-- Description (Required) -->
                <TextBlock Text="תיאור (חובה) *"
                          FontWeight="Bold" Margin="0,0,0,5"
                          HorizontalAlignment="Right"/>
                <TextBox x:Name="DescriptionTextBox"
                        FontSize="14" Padding="8" Margin="0,0,0,15"/>

                <!-- Notes (Required) -->
                <TextBlock Text="הערות (חובה) *"
                          FontWeight="Bold" Margin="0,0,0,5"
                          HorizontalAlignment="Right"/>
                <TextBox x:Name="NotesTextBox"
                        FontSize="14" Padding="8" Margin="0,0,0,15"
                        TextWrapping="Wrap" AcceptsReturn="True"
                        Height="100"/>

                <!-- Category (Optional) -->
                <TextBlock Text="קטגוריה (אופציונלי)"
                          FontWeight="Bold" Margin="0,0,0,5"
                          HorizontalAlignment="Right"/>
                <ComboBox x:Name="CategoryComboBox"
                         FontSize="14" Padding="8" Margin="0,0,0,15"
                         IsEditable="True"/>

                <!-- Info Box -->
                <Border Background="#E8F8F5" BorderBrush="#16A085" BorderThickness="2"
                       Padding="15" Margin="0,10,0,0">
                    <StackPanel>
                        <TextBlock Text="ℹ️ מידע" FontWeight="Bold" FontSize="14"
                                  HorizontalAlignment="Right" Margin="0,0,0,10"/>
                        <TextBlock TextWrapping="Wrap" HorizontalAlignment="Right">
                            <Run Text="• שינויים יישמרו מיד"/>
                            <LineBreak/>
                            <Run Text="• מספר החלק (Part Number) לא ניתן לשינוי"/>
                            <LineBreak/>
                            <Run Text="• החלק ממשיך להיות זמין בכל החיפושים"/>
                        </TextBlock>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Buttons -->
        <Border Grid.Row="2" Background="White" Padding="20" BorderBrush="#BDC3C7" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <Button Content="💾 שמור שינויים"
                       Click="SaveButton_Click"
                       Background="#27AE60" Foreground="White"
                       FontSize="14" FontWeight="Bold"
                       Padding="20,10" Margin="0,0,10,0"/>

                <Button Content="✗ ביטול"
                       Click="CancelButton_Click"
                       Background="#95A5A6" Foreground="White"
                       FontSize="14"
                       Padding="20,10"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
