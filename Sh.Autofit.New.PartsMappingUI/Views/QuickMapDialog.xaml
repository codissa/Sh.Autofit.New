<Window x:Class="Sh.Autofit.New.PartsMappingUI.Views.QuickMapDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="מיפוי מהיר - בחר חלקים"
        Height="600" Width="800"
        WindowStartupLocation="CenterOwner"
        FlowDirection="RightToLeft"
        Background="#F5F5F5">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0"
                   Text="בחר חלקים למיפוי לרכב"
                   FontSize="18"
                   FontWeight="Bold"
                   Margin="0,0,0,15"/>

        <!-- Search Box -->
        <GroupBox Grid.Row="1" Header="חיפוש חלקים" Padding="10" Margin="0,0,0,10">
            <StackPanel>
                <TextBox x:Name="SearchTextBox"
                         FontSize="14"
                         Padding="8"
                         TextChanged="SearchTextBox_TextChanged"
                         VerticalContentAlignment="Center"/>
                <TextBlock Text="חפש לפי קוד יצרן, שם חלק, קטגוריה או תת-קטגוריה"
                           FontSize="11"
                           Foreground="#666"
                           Margin="0,5,0,0"/>
            </StackPanel>
        </GroupBox>

        <!-- Parts List -->
        <Border Grid.Row="2"
                Background="White"
                BorderBrush="#DDD"
                BorderThickness="1"
                CornerRadius="4">
            <Grid>
                <DataGrid x:Name="PartsDataGrid"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          SelectionMode="Extended"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          AlternatingRowBackground="#F9F9F9"
                          IsReadOnly="False">
                    <DataGrid.Columns>
                        <DataGridCheckBoxColumn Header="✓" Binding="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" Width="40"/>
                        <DataGridTextColumn Header="קוד חלק" Binding="{Binding PartNumber}" Width="120" IsReadOnly="True"/>
                        <DataGridTextColumn Header="שם חלק" Binding="{Binding PartName}" Width="*" IsReadOnly="True"/>
                        <DataGridTextColumn Header="קטגוריה" Binding="{Binding Category}" Width="150" IsReadOnly="True"/>
                        <DataGridTextColumn Header="יצרן" Binding="{Binding Manufacturer}" Width="150" IsReadOnly="True"/>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Loading Indicator -->
                <Border Background="#F0F0F0"
                        Opacity="0.9"
                        Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <ProgressBar IsIndeterminate="True" Width="200" Height="4"/>
                        <TextBlock Text="טוען חלקים..." FontSize="14" Margin="0,10,0,0" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <!-- Selection Info -->
        <TextBlock Grid.Row="3"
                   x:Name="SelectionInfoText"
                   FontSize="13"
                   FontWeight="Bold"
                   Foreground="#0066CC"
                   Margin="0,10,0,0"
                   Text="לא נבחרו חלקים"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Margin="0,15,0,0">
            <Button Content="✓ מפה חלקים נבחרים"
                    x:Name="MapButton"
                    Click="MapButton_Click"
                    IsEnabled="False"
                    Padding="20,10"
                    FontSize="14"
                    FontWeight="Bold"
                    Margin="5"
                    Background="#4CAF50"
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="4"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#CCC"/>
                                <Setter Property="Foreground" Value="#666"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#45A049"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

            <Button Content="✗ ביטול"
                    Click="CancelButton_Click"
                    Padding="20,10"
                    FontSize="14"
                    Margin="5"
                    Background="#F44336"
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="4"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#D32F2F"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

            <Button Content="☑ בחר הכל"
                    Click="SelectAllButton_Click"
                    Padding="15,10"
                    FontSize="13"
                    Margin="5"/>

            <Button Content="☐ נקה הכל"
                    Click="ClearAllButton_Click"
                    Padding="15,10"
                    FontSize="13"
                    Margin="5"/>
        </StackPanel>
    </Grid>
</Window>
