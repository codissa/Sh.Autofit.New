<Window x:Class="Sh.Autofit.New.PartsMappingUI.Views.CopyMappingDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="העתק מיפוי חלקים"
        Height="600" Width="900"
        WindowStartupLocation="CenterOwner"
        FlowDirection="RightToLeft"
        ResizeMode="CanResize">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="העתק מיפוי חלקים מרכב למספר רכבים"
                   FontSize="16" FontWeight="Bold" Margin="0,0,0,15"/>

        <!-- Source Vehicle Section -->
        <Border Grid.Row="1" Background="#F0F8FF" Padding="10" Margin="0,0,0,10" CornerRadius="4">
            <StackPanel>
                <TextBlock Text="רכב מקור (להעתיק ממנו):" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <TextBox x:Name="SourceSearchBox"
                         Padding="8"
                         Margin="0,0,0,10"
                         TextChanged="SourceSearchBox_TextChanged">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Style.Triggers>
                                <Trigger Property="Text" Value="">
                                    <Setter Property="Background">
                                        <Setter.Value>
                                            <VisualBrush Stretch="None" AlignmentX="Right">
                                                <VisualBrush.Visual>
                                                    <TextBlock Text="🔍 חפש רכב מקור..." Foreground="Gray" Margin="0,0,5,0"/>
                                                </VisualBrush.Visual>
                                            </VisualBrush>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>
            </StackPanel>
        </Border>

        <ListBox x:Name="SourceVehicleList"
                 Grid.Row="2"
                 Margin="0,0,0,10"
                 SelectionMode="Single"
                 SelectionChanged="SourceVehicleList_SelectionChanged">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Padding="8" Margin="2" Background="#FAFAFA" CornerRadius="3" BorderBrush="#E0E0E0" BorderThickness="1">
                        <StackPanel>
                            <TextBlock FontWeight="SemiBold" FontSize="12">
                                <Run Text="{Binding ManufacturerShortName}"/>
                                <Run Text="{Binding ModelName}"/>
                                <Run Text="{Binding CommercialName, StringFormat=' ({0})'}"/>
                            </TextBlock>
                            <TextBlock Text="{Binding YearDisplay, Mode=OneWay}" Foreground="#555" FontSize="11"/>
                            <TextBlock Text="{Binding PartsCountDisplay, Mode=OneWay}" Foreground="Gray" FontSize="10"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Target Vehicles Section -->
        <Border Grid.Row="3" Background="#FFF0F5" Padding="10" Margin="0,0,0,10" CornerRadius="4">
            <StackPanel>
                <TextBlock Text="רכבים יעד (להעתיק אליהם):" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <TextBox x:Name="TargetSearchBox"
                         Padding="8"
                         Margin="0,0,0,10"
                         TextChanged="TargetSearchBox_TextChanged">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Style.Triggers>
                                <Trigger Property="Text" Value="">
                                    <Setter Property="Background">
                                        <Setter.Value>
                                            <VisualBrush Stretch="None" AlignmentX="Right">
                                                <VisualBrush.Visual>
                                                    <TextBlock Text="🔍 חפש רכבי יעד..." Foreground="Gray" Margin="0,0,5,0"/>
                                                </VisualBrush.Visual>
                                            </VisualBrush>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>
            </StackPanel>
        </Border>

        <ListBox x:Name="TargetVehicleList"
                 Grid.Row="4"
                 Margin="0,0,0,15"
                 SelectionMode="Multiple">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Padding="8" Margin="2" Background="#FAFAFA" CornerRadius="3" BorderBrush="#E0E0E0" BorderThickness="1">
                        <StackPanel>
                            <TextBlock FontWeight="SemiBold" FontSize="12">
                                <Run Text="{Binding ManufacturerShortName}"/>
                                <Run Text="{Binding ModelName}"/>
                                <Run Text="{Binding CommercialName, StringFormat=' ({0})'}"/>
                            </TextBlock>
                            <TextBlock Text="{Binding YearDisplay, Mode=OneWay}" Foreground="#555" FontSize="11"/>
                            <TextBlock Text="{Binding PartsCountDisplay, Mode=OneWay}" Foreground="Gray" FontSize="10"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Buttons -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Left">
            <Button Content="העתק מיפוי"
                    Click="CopyButton_Click"
                    Padding="15,8"
                    Margin="0,0,10,0"
                    Background="#4CAF50"
                    Foreground="White"
                    BorderThickness="0"
                    FontWeight="SemiBold"
                    Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="4"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#45A049"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            <Button Content="ביטול"
                    Click="CancelButton_Click"
                    Padding="15,8"
                    Background="#E0E0E0"
                    Foreground="Black"
                    BorderThickness="0"
                    Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="4"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#D0D0D0"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Grid>
</Window>
